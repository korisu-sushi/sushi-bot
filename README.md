# Sushi Bot 🍣

Telegram бот для суши-ресторана с возможностью просмотра меню, оформления заказа и сохранения в Google Sheets.

## Быстрый старт

### 1. Создание бота в Telegram

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "Sushi Master")
4. Введите username бота (например: `sushi_master_bot`)
5. Скопируйте токен, который выдаст BotFather

### 2. Установка зависимостей

```bash
cd sushi_bot

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

# Установить зависимости
pip install -r requirements.txt
```

### 3. Настройка конфигурации

```bash
# Скопировать шаблон
cp .env.dist .env

# Отредактировать .env
nano .env  # или любой редактор
```

В файле `.env` укажите:
```
BOT_TOKEN=ваш_токен_от_BotFather
GOOGLE_SPREADSHEET_ID=id_вашей_таблицы  # опционально
```

### 4. Запуск бота

```bash
python bot.py
```

Бот должен запуститься и вывести:
```
Bot: @your_bot_username (Bot Name)
Starting polling...
```

## Настройка Google Sheets (опционально)

Если хотите сохранять заказы в Google Sheets:

### 1. Создайте проект в Google Cloud Console

1. Перейдите на [console.cloud.google.com](https://console.cloud.google.com)
2. Создайте новый проект
3. Включите Google Sheets API и Google Drive API

### 2. Создайте сервисный аккаунт

1. IAM & Admin → Service Accounts → Create Service Account
2. Скачайте JSON-ключ
3. Сохраните его как `credentials/service_account.json`

### 3. Создайте Google Sheets таблицу

1. Создайте новую таблицу в Google Sheets
2. Скопируйте ID таблицы из URL (между `/d/` и `/edit`)
3. Дайте доступ на редактирование email сервисного аккаунта (из JSON-файла)

### 4. Добавьте настройки в .env

```
GOOGLE_SPREADSHEET_ID=your_spreadsheet_id_here
GOOGLE_CREDENTIALS_PATH=credentials/service_account.json
```

## Структура меню

Меню хранится в `data/menu.json`. Вы можете изменить его структуру:

```json
{
  "categories": [
    {
      "id": "rolls",
      "name": "Роллы",
      "emoji": "🍣",
      "items": [
        {
          "id": "roll_001",
          "name": "Филадельфия",
          "description": "Лосось, сливочный сыр",
          "price": 450,
          "weight": "250г",
          "pieces": 8
        }
      ]
    }
  ]
}
```

## Функционал

- ✅ Просмотр меню по категориям
- ✅ Добавление в корзину с выбором количества
- ✅ Управление корзиной (изменение количества, удаление)
- ✅ Оформление заказа (имя, телефон, адрес, время)
- ✅ Сохранение заказов в Google Sheets
- ✅ Расчёт стоимости доставки

## Будущие улучшения

- [ ] Уведомления на кухню (отдельный бот)
- [ ] Онлайн-оплата (Telegram Payments)
- [ ] История заказов
- [ ] Админ-панель

## Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота
2. Наличие всех файлов в нужных папках
3. Правильность JSON-структуры меню
